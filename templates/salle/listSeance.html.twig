{% extends 'base.html.twig' %}

{% block title %}Seance de la Salle {{ salle.id }} {% endblock %}

{% block body %}
    <h1>Création de la Seance {{ salle.id }}</h1>

{% for seance in seances %}
            <tr>
                <td>{{ seance.id }}</td>
                <td>{{ seance.Film.title }}</td>
                <td>{{ seance.dateDebut ? seance.dateDebut|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ seance.langue }}</td>
                <td>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>



{% endblock %}